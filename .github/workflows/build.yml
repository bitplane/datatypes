name: AROS datatype checks

on:
  push:
    branches: [master]
    tags: ['*-*.*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  decoder:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Check release tag
        if: startsWith(github.ref, 'refs/tags/')
        run: sh scripts/tag-package.sh "$GITHUB_REF_NAME"
      - run: make test

  sdk:
    name: ${{ matrix.package }}-${{ matrix.target }}
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        package: [targa, pcx, qoi]
        target: [i386-aros, aarch64-aros, x86_64-aros, m68k-aros]
        include:
          - target: i386-aros
            toolbox: aros/2026-08-31/i386:v0.1.1
          - target: aarch64-aros
            toolbox: aros/2026-08-31/aarch64:v0.1.1
          - target: x86_64-aros
            toolbox: aros/2026-08-31/x86_64:v0.1.8
          - target: m68k-aros
            toolbox: aros/2026-08-31/m68k:v0.1.9
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v6
      - name: Check release tag
        if: startsWith(github.ref, 'refs/tags/')
        run: sh scripts/tag-package.sh "$GITHUB_REF_NAME"
      - name: Build datatype
        if: ${{ !startsWith(github.ref, 'refs/tags/') || startsWith(github.ref_name, format('{0}-', matrix.package)) }}
        env:
          GH_TOKEN: ${{ github.token }}
          TOOLBOX: ${{ matrix.toolbox }}
          TARGET: ${{ matrix.target }}
          PACKAGE: ${{ matrix.package }}
        run: |
          image="ghcr.io/bitplane/mountin/builder/compiler/$TOOLBOX"
          printf '%s' "$GH_TOKEN" | docker login ghcr.io \
              --username "$GITHUB_ACTOR" --password-stdin
          docker pull "$image"
          docker run --rm --volume "$PWD:/work" --workdir /work \
              "$image" sh scripts/build.sh "$TARGET" "$PACKAGE"
      - name: Package datatype
        if: ${{ !startsWith(github.ref, 'refs/tags/') || startsWith(github.ref_name, format('{0}-', matrix.package)) }}
        env:
          PACKAGE: ${{ matrix.package }}
          TARGET: ${{ matrix.target }}
        run: |
          sudo chown -R "$(id -u):$(id -g)" dist
          version=$(sed -n 's/^version //p' "formats/$PACKAGE/$PACKAGE.conf")
          sh scripts/aros/package.sh "$TARGET" "$PACKAGE" "$version"
      - uses: actions/upload-artifact@v7
        if: ${{ !startsWith(github.ref, 'refs/tags/') || startsWith(github.ref_name, format('{0}-', matrix.package)) }}
        with:
          name: ${{ matrix.package }}-${{ matrix.target }}
          path: |
            dist/${{ matrix.target }}/${{ matrix.package }}.datatype
            dist/*.tar.bz2*
          if-no-files-found: error

  release:
    needs: [decoder, sdk]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
      - uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            artifacts/**/*.tar.bz2
            artifacts/**/*.sha256

  sync-pkg:
    needs: release
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
    uses: ./.github/workflows/sync-aros.yml
    with:
      tag: ${{ github.ref_name }}
    secrets:
      AROS_PKG_KEY: ${{ secrets.AROS_PKG_KEY }}
