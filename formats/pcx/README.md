# PCX

Reads packed and planar indexed PCX, 8-bit indexed PCX and 24-bit RGB PCX.

Saves 24-bit RGB PCX, with transparency composited over white.

AROS's own `Devs/DataTypes/PCX` descriptor selects the class.
