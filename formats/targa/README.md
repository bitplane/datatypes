# Targa

Reads colour-mapped, true-colour and greyscale TGA, raw or RLE.

Saves RLE true-colour TGA: 24-bit, or 32-bit if the picture has transparency.

AROS's own `Devs/DataTypes/Targa` descriptor selects the class.
